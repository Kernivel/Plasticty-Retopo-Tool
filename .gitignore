__pycache__/
*.py[cod]
*.blend1
.DS_Store

# JetBrains IDE settings
.idea/

# Plasticity's own backup folder next to the fixture source: autosave, hourly
# and daily copies. The deliberate save is tests/fixtures/TestCases.plasticity.
tests/fixtures/plasticity/

# MkDocs build output (docs/ is the source, site/ is generated).
site/

# Release zips (scripts/build_zip.py). Built from a tag, attached to the
# GitHub release; never committed.
dist/
